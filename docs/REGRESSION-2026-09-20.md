# Codex TouchBar Monitor 全量回归记录（2026-09-20）

## 结论
自动检查全部通过；单/双击事件、多任务真实状态和关闭后重开已检查。
本轮不是“所有 PRD 条目均已真机验收”：未覆盖项见下表。
文字输入/中文 IME 已按用户要求暂停，不属于当前可用功能。

## 本轮修复
- 双击手动打开任务区不再受自动显示条件限制；无任务时也能打开空状态。
- 系统 X 关闭后同步清除显示标记，双击重新建立并呈现任务栏。
- 按用户要求移除重复的「‹ App」，以系统 X 返回前台应用原生控件。
- 增加只读本地会话索引补充：独立 App Server 的列表未包含本轮创建的会话。
  索引仅读取未归档会话 ID；运行状态、完成状态和决策仍来自 Desktop IPC。
  仅在连接及既有发现触发点读取，无高频状态轮询，不修改 Codex 数据库。
- 修复空标题：忽略空 title、回退 generatedTitle/默认名，并在后续快照到达时更新已有任务标题。
  实测确认布局尝试没有解决只剩图标的问题，已撤回该尝试。
- 保留已修复的旧按钮重复触发防护；文字回复入口仅打开 Codex，不接管键盘。

## 自动检查（全部在 build host）
81 项 core + 56 项 integration + 23 项 AppKit，共 160 项通过。
包含状态归属、额度/网络状态、断线和超时、WebSocket 分片、
Desktop IPC 精确目标与 Unicode 响应、重复/过期按钮、请求清理、
单击延迟与双击取消单击、只读会话索引过滤及不修改数据库。
输入内部组件仍有历史测试覆盖，但不代表当前产品启用了文本输入。
Release 构建、打包及签名校验通过；本机未写源码或编译。

## 真机 / 真实会话
| 项目 | 结果与证据 |
| --- | --- |
| 状态块单击 | 收到 single 事件，Codex activate 返回 true，未改变绑定 |
| 双击 → X → 再双击 | 用户明确确认“三步都正常” |
| 移除重复返回入口 | 用户指定；Touch Bar 截图仅保留系统 X |
| 网络与周额度 | 多轮截图持续显示实时延迟及周剩余百分比 |
| 多任务发现 | 两个测试会话及当前开发任务收到独立 Desktop 快照 |
| 运行 → 完成 | 已拍摄同时运行的黄色任务及随后绿色完成任务 |
| 新一轮任务 | 在同一测试会话继续后收到新轮次状态；旧完成轮次保留待确认 |
| 文字回复 | 回归断言确认打开 Codex 且不捕获键盘、不代发答案 |
| 决策选项 / 审批 | 本轮离线模拟通过；此前真实 Touch Bar 演示通过，本轮未取得真实审批请求验收 |
| 普通异步提问 | 测试 C 发出问题，未形成可验证的原生待审批回送闭环；不能标记通过 |

## 测试会话
- A: <test-task-id>
- B: <test-task-id>
- C: <test-task-id>

会话只等待或提出测试选项，没有在本机写源码、安装依赖或编译。
测试过程中没有批准或拒绝用户真实工作请求。

## 尚未覆盖 / 风险
- 真实审批/权限请求的端到端回送，普通异步提问的兼容性。
- 远端主机任务发现：当前 Desktop IPC 路径仅处理 hostId=local。
- 实际断网、退出 Codex 后重新启动、登录启动、休眠唤醒和 ControlStrip 重启。
  本轮没有为测试打断用户现有 Codex 会话或修改系统网络。
- 私有 Touch Bar API、Desktop IPC 版本和本地索引结构可能随系统/应用升级变化。
- 中文 IME/自由文字输入明确暂停，不承诺可用。

## 当前包
版本仍为开发中的 1.1.0，未发布新 GitHub Release。
SHA256: 426d8eb5704606177c8ac2e42821d33572bf80188d9b1cac22b01b23b0a93d9a
本机运行真实模式，无演示决策。

## 收尾补充
最终标题修复通过 3 项新增回归断言，已部署；该补丁之后没有再要求用户重复真机操作。
直接运行最终包已确认网络/额度恢复（约 495ms、36%）；使用 LaunchServices 重启的
一次尝试中状态块没有恢复，重新直接运行后正常。这是启动/注册生命周期的待复查项，
不能把“所有启动方式均通过”作为本轮结论。
测试 A、B、C 已全部结束，无后台测试等待任务继续运行。

## 1.1.1 发布回归

- 91 核心断言、57 集成断言、23 原生 UI 断言通过。
- 新增异步提问、精确问题 ID、选项映射、已接受/未确认回复、终态清理及等待标志覆盖。
- 手动打开安装后的 App 显示状态窗口与实时网络/额度，再次打开可显示窗口。
- 用户确认真实 Touch Bar 选项和闪烁三角正常，对应问题回复已收到。
- 当前版本个人路径、开发机器标识、测试任务 ID 已清理，历史提交按用户要求保留。
- ZIP 包逐项扫描未包含个人目录/机器名；签名校验通过。
