# Codex TouchBar Monitor 1.1.1

这是可放入“应用程序”后双击运行的原生后台 App。关闭状态窗口不会退出，菜单栏 CTB 可管理或退出。

- 修复手动启动初始化，并支持再次打开显示状态窗口。
- 保留已验收的单击打开 Codex、双击进入任务区、系统 X 退出后再次进入。
- 识别异步提问及其选项；等待回复时显示闪烁三角，已接受的回复只清除对应问题。
- 同时尊重运行时等待输入/审批标志，避免错误显示为执行中。
- 清理当前源码和文档中的开发机器名、个人目录、测试任务编号；构建配置仅留在被忽略的本地文件中。历史提交保留。
- 发布二进制去除调试符号并映射编译路径；发行包不包含日志、开发配置或账号数据。

自动测试：91 项核心断言、57 项集成断言、23 项原生 UI 断言通过。构建和测试均在配置的远端构建机器完成。
真实硬件：常驻数据显示、双击/X 回路已验收；本版手动打开显示实时状态已验证。用户确认异步选项和闪烁三角正常，对应真实回复已收到。

要求 macOS 13+ / Apple Silicon。仅 ad-hoc 签名，未公证。ZIP 解压后拖入“应用程序”。
自由文本/中文 IME 输入已暂停，请通过 Open Codex 回复。远端主机发现、真实审批完整回路和长期休眠恢复尚未验收。私有接口可能随系统或 Codex 更新改变。
